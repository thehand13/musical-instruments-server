### MUSICAL INSTRUMENTS SERVER

## Install dependencies:

npm install

## Start in developer mode:

npm run start:dev

## Start in developer mode using Docker:

docker-compose build

docker-compose up

## Start in production mode:

npm run start:prod
